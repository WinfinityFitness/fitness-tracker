<?xml version="1.0" encoding="utf-8"?>
<!-- A set is actively in progress — shows the current exercise + timer
     live, pushed from initTrainingWidgetSync in app.js. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@drawable/widget_background">
    <TextView
        android:id="@+id/widgetExerciseName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#DDE3E5"
        android:textSize="14sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />
    <TextView
        android:id="@+id/widgetTimerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="#33C8CC"
        android:textSize="20sp"
        android:textStyle="bold" />
    <ImageButton
        android:id="@+id/widgetFinishIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginTop="8dp"
        android:background="@android:color/transparent"
        android:src="@drawable/widget_icon_finish"
        android:scaleType="fitCenter"
        android:padding="4dp"
        android:contentDescription="Finish" />
</LinearLayout>
